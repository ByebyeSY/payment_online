{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "ihm:dev" : "lerna run --scope=ihm dev",
    "api:dev" : "lerna run --scope=api start:dev",
    "build" : "lerna run build"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^7.4.2"
  }
}
